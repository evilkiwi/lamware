{
    "private": true,
    "workspaces": [
        "example/*",
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build --stream",
        "lint": "lerna run lint --stream",
        "test": "lerna run test --stream",
        "prune": "lerna clean && rm -rf node_modules",
        "example": "yarn workspace @lamware/example",
        "example-stack": "yarn workspace @lamware/example-stack",
        "lamware": "yarn workspace @lamware/core"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.14.0",
        "@typescript-eslint/parser": "^5.14.0",
        "@vue/eslint-config-typescript": "^10.0.0",
        "eslint-plugin-vue": "^8.5.0",
        "lerna": "^4.0.0",
        "typescript": "^4.6.2",
        "vue-eslint-parser": "^8.3.0"
    }
}
